# xentra_backend
git init
git add .
git commit -m "Initial backend setup with auth"
git branch -M main
git remote add origin https://github.com/<samarthdeshpande2006>/xentra-backend.git
git push -u origin main
